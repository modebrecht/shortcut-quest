name: One-off Modern Report Link

on:
  push:
    branches: [tk2-2026]
    paths:
      - '.github/workflows/oneoff-modern-report-link.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tk2-2026
      - name: Apply report UI loader
        run: python3 2026/qa/apply_modern_report_link.py
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add 2026/skill-hotkeys.js
          git commit -m "A8: load modern report styles"
          git push origin HEAD:tk2-2026
